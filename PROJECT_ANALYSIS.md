# Анализ проекта Ai- (Chat System)

## Обзор проекта

Это система чата для имитации общения с виртуальным персонажем (по умолчанию "Анна", 28 лет) на сайте знакомств. Система использует локальную LLM через Ollama для генерации ответов.

## Архитектура

```
chat_system/
├── server.py          # FastAPI веб-сервер + HTML интерфейс
├── database.py        # SQLite база данных
├── rag.py            # ChromaDB для RAG (отключено)
├── analyzer.py       # Анализ сообщений (имя, настроение)
├── training.py       # Экспорт данных для обучения
├── prompt_manager.py # Управление промптами
├── config.py         # Конфигурация
└── requirements.txt  # Зависимости
```

## Компоненты

### 1. server.py
- **FastAPI** веб-сервер на порту 8000
- Встроенный HTML/CSS/JS интерфейс
- Эндпоинты:
  - `/chat` - отправка сообщения
  - `/rate` - оценка ответа (👍/👎)
  - `/correct` - исправление ответа
  - `/regenerate` - перегенерация ответа
  - `/export` - экспорт для обучения
  - `/prompt` - управление промптом
  - `/import/rag` - импорт примеров в RAG

### 2. database.py
- **SQLite** база данных
- Таблицы:
  - `users` - профили собеседников
  - `messages` - история сообщений с оценками/исправлениями
  - `exports` - записи экспортов

### 3. rag.py
- **ChromaDB** для векторного поиска похожих примеров
- **Текущий статус: ОТКЛЮЧЕНО** (RAG_ENABLED = False)
- Причина: нестабильность модели при использовании RAG

### 4. analyzer.py
- Определение имени собеседника (regex + LLM fallback)
- Определение настроения по ключевым словам

### 5. training.py
- Экспорт в формате **ShareGPT** для fine-tuning
- Инструкция обучения на RunPod с Unsloth

### 6. prompt_manager.py
- Настраиваемый системный промпт
- Поля: имя, возраст, описание, стиль, разрешённые/запрещённые темы

## Текущие настройки (config.py)

```python
MODEL_NAME = "dolphin-llama3"    # Модель Ollama
RAG_ENABLED = False              # RAG отключён
MAX_CONTEXT_MESSAGES = 10        # Сообщений в контексте
```

## Рабочий процесс обучения

### Вариант 1: RAG (мгновенное улучшение)
1. Добавление примеров через UI (вкладка 📥)
2. Примеры сохраняются в ChromaDB
3. При новом сообщении ищутся похожие примеры
4. Примеры добавляются в промпт

**Статус**: Отключено из-за проблем с качеством ответов

### Вариант 2: Fine-tuning (рекомендуется)
1. Оценивайте ответы (👍) или исправляйте (✏️)
2. Экспортируйте данные (вкладка 🎓)
3. Обучите модель на RunPod (инструкция в UI)
4. Замените модель в Ollama

## Известные проблемы и решения

### Проблема: Модель говорит за обоих персонажей
**Решение**: Добавлены stop-токены в Ollama API:
```python
"stop": ["Мужчина:", "\nМужчина:", "Human:", ...]
```

### Проблема: Модель выводит технические маркеры
**Решение**: Функция `clean_response()` фильтрует:
- "Ты:" в начале
- Разделители "---"
- Повторяющиеся предложения

### Проблема: Модель выходит из роли
**Решение**:
- Упрощён промпт
- Отключён RAG
- Рекомендуется dolphin-llama3 вместо qwen

## Рекомендации по улучшению

### Для стабильности
1. ✅ RAG отключён
2. ✅ Упрощённый промпт
3. ✅ Stop-токены настроены
4. ✅ Очистка ответов

### Для обучения
1. Собирайте качественные диалоги через UI
2. Используйте fine-tuning вместо RAG
3. Формат данных: ShareGPT JSON
4. Платформа: RunPod + Unsloth + LoRA

### Формат данных для обучения
```json
[
  {
    "conversations": [
      {"from": "human", "value": "Привет"},
      {"from": "gpt", "value": "Привет! Как дела?"},
      {"from": "human", "value": "Хорошо, а ты как?"},
      {"from": "gpt", "value": "Тоже хорошо, думаю о тебе 😊"}
    ]
  }
]
```

## Запуск проекта

```bash
# Установка зависимостей
cd chat_system
pip install -r requirements.txt

# Установка модели
ollama pull dolphin-llama3

# Запуск
python server.py

# Открыть http://127.0.0.1:8000
```

## Выводы

Проект представляет собой рабочую систему для:
1. Общения с виртуальным персонажем
2. Сбора и оценки ответов
3. Экспорта данных для fine-tuning

Основной путь улучшения качества — fine-tuning на собранных данных, а не RAG.
